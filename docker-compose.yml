version: '2'

services:
  db:
    image: postgres
    volumes:
      - database-data:/var/lib/postgresql/data

  web:
    build: .
    entrypoint: /entrypoint.sh
    volumes:
      - .:/code
    ports:
      - "80:80"
    depends_on:
      - db

volumes:
  database-data:
